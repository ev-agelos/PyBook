stages:
- test
- build
- deploy

run_tests:
  stage: test
  image: python:3.5.2
  before_script:
  - "pip install -e ."
  - "pip install pytest-cov"
  - "pip install codecov"
  script:
  - py.test -nauto --cov bookmarks
  after_script:
  - codecov --token=$CODECOV_TOKEN

build_image:
  stage: build
  image: docker:latest
  services:
  - docker:dind
  script:
  - docker login -u gitlab-ci-token -p $CI_BUILD_TOKEN registry.gitlab.com
  - docker build -t registry.gitlab.com/evagelos/pybook .
  - docker push registry.gitlab.com/evagelos/pybook:latest
  only:
  - master

deploy_image:
  stage: deploy
  before_script:
  - 'which ssh-agent || ( apt-get update -y && apt-get install openssh-client -y )'
  - eval $(ssh-agent -s)
  - ssh-add <(echo "$SSH_PRIVATE_KEY")
  - mkdir -p ~/.ssh
  - '[[ -f /.dockerenv ]] && echo -e "Host *\n\tStrictHostKeyChecking no\n\n" > ~/.ssh/config'
  script:
  - ssh $SERVER './deploy.sh' $S2G_USERNAME $S2G_PASSWORD
  environment:
    name: production
    url: https://pybook.site
  only:
  - master
