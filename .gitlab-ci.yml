stages:
- test
- build
- deploy

run_tests:
  stage: test
  image: python:3.8
  before_script:
  - "pip install -e ."
  - "pip install pytest pytest-xdist pytest-cov codecov"
  script:
  - py.test -n auto --cov bookmarks
  after_script:
  - codecov --token=$CODECOV_TOKEN

build_image:
  stage: build
  image: docker:latest
  services:
  - docker:dind
  script:
  - docker login -u gitlab-ci-token -p $CI_BUILD_TOKEN registry.gitlab.com
  - docker build -t registry.gitlab.com/evagelos/pybook .
  - docker push registry.gitlab.com/evagelos/pybook:latest
  only:
  - master

deploy_image:
  stage: deploy
  before_script:
  - 'which ssh-agent || ( apt-get update -y && apt-get install openssh-client -y )'
  - eval $(ssh-agent -s)
  - echo "$SSH_PRIVATE_KEY" | tr -d '\r' | ssh-add -
  - mkdir -p ~/.ssh
  - chmod 700 ~/.ssh
  - echo "$SSH_KNOWN_HOSTS" > ~/.ssh/known_hosts
  - chmod 644 ~/.ssh/known_hosts
  script:
  - ssh $SERVER "echo $CI_BUILD_TOKEN | ./deploy.sh" gitlab-ci-token
  after_script:
  - curl $WEBSITE_URL -fL
  environment:
    name: production
    url: $WEBSITE_URL
  only:
  - master


include:
  - template: Dependency-Scanning.gitlab-ci.yml
  - template: SAST.gitlab-ci.yml
